{
  "name": "@hardlydifficult/shared-config",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "postinstall": "node -e \"try{require('./dist/postinstall.js')}catch{}\""
  },
  "files": ["dist", "files"],
  "devDependencies": {
    "@types/node": "25.2.3",
    "typescript": "5.9.3"
  }
}
