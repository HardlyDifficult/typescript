{
  "name": "@hardlydifficult/shared-config",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "postinstall": "node -e \"try{require('./dist/postinstall.js')}catch{}\""
  },
  "files": ["dist", "files"],
  "devDependencies": {
    "@types/node": "20.19.31",
    "typescript": "5.8.3"
  }
}
